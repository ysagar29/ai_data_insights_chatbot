You are a SQL expert. Generate a PostgreSQL CREATE TABLE statement based on the provided CSV headers and sample data.

Table Name: {tableName}
CSV Headers: {headers}
Sample Data (first few rows):
{sampleData}

Requirements:
1. Analyze the CSV headers and sample data to infer appropriate data types
2. Use meaningful column names (convert CSV headers to valid SQL column names if needed)
3. Add a primary key column named 'id' with SERIAL type
4. Choose appropriate data types (TEXT, INTEGER, DECIMAL, DATE, TIMESTAMP, etc.) based on the data
5. Add NOT NULL constraints where appropriate
6. If you see date-like values, use DATE or TIMESTAMP type
7. For numeric values, use INTEGER or DECIMAL
8. For text values, use TEXT
9. Create the table with IF NOT EXISTS clause

Return ONLY the CREATE TABLE SQL statement, without any explanation or markdown formatting.

